PACKAGE="deptestb"

# Check dependencies
dependencies=(deptestc)
CheckDeps

# We should redefine the package
PACKAGE="deptestb"

# Build the package
source ${PORTS}/${PACKAGE}/build

# Installs the package
source ${PORTS}/${PACKAGE}/install

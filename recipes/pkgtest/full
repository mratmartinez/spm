PACKAGE="pkgtest"

# Check dependencies
source ${PORTS}/${PACKAGE}/deps

CheckDeps

# We should redefine de PACKAGE
PACKAGE="pkgtest"

# Build the package
source ${PORTS}/${PACKAGE}/build

# Installs the package
source ${PORTS}/${PACKAGE}/install
